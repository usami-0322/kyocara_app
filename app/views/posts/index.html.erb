<div class="main posts-index">
  <div class="container">
    <h2 classs="form-head">みんなの投稿</h2>
    <% @posts.each do |post| %>
    <div class="post-index-item">
    <%= link_to(post.content, "/posts/#{id}) %>
    </div>
    <% end %>
  </div>
</div>
